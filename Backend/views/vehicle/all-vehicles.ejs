<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= pageTitle %></title>
</head>

<body>
    <h1><%= pageTitle %></h1>
    <form action="/admin/vehicle/read" method="GET"></form>

    <% if (vehicles && vehicles.length > 0) { %>
        <table border="1">
            <thead>
                <tr>
                    <th>VehicleID</th>
                    <th>VehicleType</th>
                    <th>Make</th>
                    <th>Model</th>
                    <th>RegistrationTag</th>
                    <th>EmplID</th>
                </tr>
            </thead>
            <tbody>
                <% vehicles.forEach(vehicle => { %>
                    <tr>
                        <td><%= vehicle.VehicleID %></td>
                        <td><%= vehicle.VehicleType %></td>
                        <td><%= vehicle.Make %></td>
                        <td><%= vehicle.Model %></td>
                        <td><%= vehicle.RegistrationTag %></td>
                        <td><%= vehicle.EmplID %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    <% } else { %>
        <h1>No Vehicles Found!</h1>
    <% } %>
    <%- include('../includes/end.ejs') %>
</html>